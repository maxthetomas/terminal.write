{
  "dependencies": {
    "@types/ssh2": "^1.15.4",
    "drizzle-orm": "^0.39.2",
    "ssh2": "^1.16.0"
  },
  "scripts": {
    "dev": "nix-shell --run 'bun run --watch src/index.ts'",
    "prod": "bun run src/index.ts",
    "push": "bun run --bun drizzle-kit push"
  },
  "devDependencies": {
    "@libsql/client": "^0.14.0",
    "@types/bun": "^1.2.2",
    "drizzle-kit": "^0.30.4"
  }
}